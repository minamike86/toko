/**
 * Enumeration representing the type of an Order. Different types
 * correspond to different sales channels or contexts. Use cases may
 * apply specific rules based on the order type.
 */

export enum OrderType {
  ONLINE = "ONLINE",
  OFFLINE = "OFFLINE",
}
